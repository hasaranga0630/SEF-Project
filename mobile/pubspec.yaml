name: sme_inventory_app
description: SME Platform inventory mobile client.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.2.2
  mobile_scanner: ^5.2.3
  connectivity_plus: ^6.1.3
  shared_preferences: ^2.3.5
  image_picker: ^1.1.2
  web_socket_channel: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
